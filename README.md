# Which Command

```
Which command I used before to achieve that? 🤔

Usage: which-cmd [OPTIONS] [SEARCH]...

Arguments:
  [SEARCH]...  Optional search for commands

Options:
  -c, --count <COUNT>    Number commands to print [default: 5]
      --exact            Use exact search match
      --fuzzy            Use fuzzy search match (default)
  -i, --ignore <IGNORE>  List of commands to ignore
  -s, --score <SCORE>    Minimum score for fuzzy search [default: 40]
  -h, --help             Print help
  -V, --version          Print version
```
